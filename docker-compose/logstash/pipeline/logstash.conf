input {
  tcp {
    port => 5000
    codec => json_lines
  }
}

filter {
  mutate {
    add_field => { "app" => "vett-gateway" }
  }
}

output {
  elasticsearch {
    hosts => ["http://elasticsearch:9200"]
    index => "vett-logs-%{+YYYY.MM.dd}"
  }
  stdout { codec => rubydebug }
}
